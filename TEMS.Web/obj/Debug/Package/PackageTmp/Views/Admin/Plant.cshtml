@{
    ViewBag.Title = "Plant Management";
}

@section scripts{
<script type="text/javascript">
    $(document).ready(function () {
        $('#jTablePlant').jtable({
            title: 'Plant Master',
            paging: true, //Enable paging
            pageSize: 10, //Set page size (default: 10)
            sorting: true, //Enable sorting
            selecting: true, //Enable selecting
            defaultSorting: 'plant_id ASC', //Set default sorting
            actions: {
                listAction: '@Url.Action("PlantList","Admin")',
                deleteAction: '@Url.Action("DeletePlant","Admin")',
                updateAction: '@Url.Action("UpdatePlant","Admin")',
                createAction: '@Url.Action("CreatePlant","Admin")'
            },
            fields: {
                plant_id: { title: 'ID', key: true, create: false, edit: false },
                plant_name: { title: 'Name', create: true, edit: true },
                active: {
                    title: 'Status', create: false, edit: true, type: 'checkbox',
                    values: { 'false': 'Inactive', 'true': 'Active' },
                    defaultValue: 'true'
                },
                update_dt: { title: 'Update', create: false, edit: false, type: 'date',displayFormat: 'dd-mm-yy' }
            }
        });
        //Load student list from server
        $('#jTablePlant').jtable('load');
    });
</script>
}

<h2 class="page-header">@ViewBag.Title</h2>

<div id="jTablePlant"></div>
