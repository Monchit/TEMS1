@{
    ViewBag.Title = "Judgement Management";
}

@section scripts{
<script type="text/javascript">
    $(document).ready(function () {
        $('#jTableJudgement').jtable({
            title: 'Judgement Master',
            paging: true, //Enable paging
            pageSize: 10, //Set page size (default: 10)
            sorting: true, //Enable sorting
            selecting: true, //Enable selecting
            defaultSorting: 'judge_id ASC', //Set default sorting
            actions: {
                listAction: '@Url.Action("JudgementList","Admin")',
                deleteAction: '@Url.Action("DeleteJudgement","Admin")',
                updateAction: '@Url.Action("UpdateJudgement","Admin")',
                createAction: '@Url.Action("CreateJudgement","Admin")'
            },
            fields: {
                judge_id: { title: 'ID', key: true, create: true, edit: false },
                judge_name: { title: 'Name', create: true, edit: true },
                //active: {
                //    title: 'Status', create: false, edit: true, type: 'checkbox',
                //    values: { 'false': 'Inactive', 'true': 'Active' },
                //    defaultValue: 'true'
                //},
                update_dt: { title: 'Update', create: false, edit: false, type: 'date',displayFormat: 'dd-mm-yy' }
            }
        });
        //Load student list from server
        $('#jTableJudgement').jtable('load');
    });
</script>
}

<h2 class="page-header">@ViewBag.Title</h2>

<div id="jTableJudgement"></div>
