@{
    ViewBag.Title = "Unit Management";
}

@section scripts{
<script type="text/javascript">
    $(document).ready(function () {
        $('#jTableUnit').jtable({
            title: 'Unit Master',
            paging: true, //Enable paging
            pageSize: 10, //Set page size (default: 10)
            sorting: true, //Enable sorting
            selecting: true, //Enable selecting
            defaultSorting: 'unit_id ASC', //Set default sorting
            actions: {
                listAction: '@Url.Action("UnitList","Admin")',
                deleteAction: '@Url.Action("DeleteUnit","Admin")',
                updateAction: '@Url.Action("UpdateUnit","Admin")',
                createAction: '@Url.Action("CreateUnit","Admin")'
            },
            fields: {
                unit_id: { title: 'ID', key: true, create: false, edit: false },
                unit_name: { title: 'Name', create: true, edit: true },
                active: {
                    title: 'Status', create: false, edit: true, type: 'checkbox',
                    values: { 'false': 'Inactive', 'true': 'Active' },
                    defaultValue: 'true'
                },
                update_dt: { title: 'Update', create: false, edit: false, type: 'date',displayFormat: 'dd-mm-yy' }
            }
        });
        //Load student list from server
        $('#jTableUnit').jtable('load');
    });
</script>
}

<h2 class="page-header">@ViewBag.Title</h2>

<div id="jTableUnit"></div>
